{
  "lang": "ja",
  "type": "node-cli",
  "concurrency": 2,
  "docs": {
    "languages": ["en", "ja"],
    "defaultLanguage": "en",
    "mode": "generate",
    "style": {
      "purpose": "user-guide",
      "tone": "polite",
      "customInstruction": ""
    }
  },
  "scan": {
    "include": ["src/**/*.js", "src/**/*.mjs", "src/**/*.cjs", "package.json"],
    "exclude": ["experimental/**"]
  },
  "flow": {
    "merge": "squash"
  },
  "agent": {
    "default": "claude",
    "workDir": ".tmp",
    "timeout": 600,
    "providers": {
      "claude": {
        "command": "claude",
        "args": ["-p", "{{PROMPT}}"],
        "systemPromptFlag": "--system-prompt",
        "profiles": {
          "default": [],
          "opus": ["--model", "opus"],
          "sonnet": ["--model", "sonnet"]
        }
      },
      "codex": {
        "command": "codex",
        "args": ["exec", "--full-auto", "-C", ".tmp", "{{PROMPT}}"],
        "profiles": {
          "default": []
        }
      }
    },
    "commands": {
      "docs.enrich":       { "agent": "claude", "profile": "sonnet" },
      "docs.text":         { "agent": "claude", "profile": "sonnet" },
      "docs.forge":        { "agent": "claude", "profile": "sonnet" },
      "docs.readme":       { "agent": "claude", "profile": "sonnet" },
      "docs.agents":       { "agent": "claude", "profile": "sonnet" },
      "spec.gate":         { "agent": "claude", "profile": "sonnet" },
      "experimental.workflow.borad.to-issue": { "agent": "claude", "profile": "sonnet" },
      "flow.review.draft": { "agent": "claude", "profile": "sonnet" },
      "flow.review.final": { "agent": "claude", "profile": "opus" },
      "docs.translate":    { "agent": "claude", "profile": "sonnet" }
    }
  }
}
